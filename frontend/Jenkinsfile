pipeline {
    agent any

    stages {
        stage('Verify Files') {
            steps {
                sh 'ls -la'
            }
        }

        stage('Deploy') {
            steps {
                sh '''
                echo "Deploying static HTML project"
                '''
            }
        }
    }
}