- name: Deploy Job Portal using Docker Compose
  hosts: app
  become: yes

  tasks:
    - name: Install Docker
      apt:
        name: docker.io
        state: present
        update_cache: yes

    - name: Install docker-compose plugin
      apt:
        name: docker-compose-plugin
        state: present

    - name: Start and enable Docker
      service:
        name: docker
        state: started
        enabled: yes

    - name: Create app directory
      file:
        path: /opt/job-portal
        state: directory
        owner: root
        group: root
        mode: '0755'

    - name: Sync project files
      synchronize:
        src: "{{ playbook_dir }}/../"
        dest: /opt/job-portal
        delete: yes
        rsync_opts:
          - "--exclude=.git"
          - "--exclude=node_modules"
          - "--exclude=Ansible"

    - name: Deploy containers
      community.docker.docker_compose_v2:
        project_src: /opt/job-portal
        build: true
        state: present
