
services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfil
    ports:
      - "5001:5000"
    environment:
      - NODE_ENV=production
    volumes:
      - ./backend:/app
      - /app/node_modules

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3030:30"
    depends_on:
      - backend
